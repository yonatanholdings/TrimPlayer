<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:clipToPadding="false"
    android:paddingBottom="20dp">

    <com.google.android.material.bottomsheet.BottomSheetDragHandleView
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Brand icon chip -->
    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="4dp"
        android:background="@drawable/bg_share_icon_circle"
        android:backgroundTint="?attr/colorSecondaryContainer">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:importantForAccessibility="no"
            app:srcCompat="@drawable/ic_share"
            app:tint="?attr/colorOnSecondaryContainer" />

    </FrameLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="14dp"
        android:gravity="center_horizontal"
        android:text="@string/share_dialog_title"
        style="@style/TextAppearance.Material3.HeadlineSmall" />

    <!-- The episode being shared -->
    <TextView
        android:id="@+id/episodeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="4dp"
        android:gravity="center_horizontal"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="?attr/colorOnSurfaceVariant"
        tools:text="Episode title"
        style="@style/TextAppearance.Material3.BodyMedium"
        xmlns:tools="http://schemas.android.com/tools" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="10dp"
        android:gravity="center_horizontal"
        android:text="@string/share_dialog_link_hint"
        android:textColor="?attr/colorOnSurfaceVariant"
        style="@style/TextAppearance.Material3.BodySmall" />

    <!-- Include-position toggle (functionality preserved) -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/share_position_checkbox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        tools:text="Share current position (00:00:16)"
        xmlns:tools="http://schemas.android.com/tools" />

    <!-- Primary action: share the episode link -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/shareLinkButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:text="@string/share_label"
        app:icon="@drawable/ic_share"
        app:iconGravity="textStart"
        style="@style/Widget.Material3.Button" />

    <!-- Secondary: share the downloaded audio file (only when downloaded) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/shareAudioFileButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:text="@string/share_dialog_audio_file"
        app:icon="@drawable/ic_download"
        app:iconGravity="textStart"
        style="@style/Widget.Material3.Button.TextButton" />

</LinearLayout>
